---
// Si alguien accede directamente a /martos-2d, redirigir a una ruta predeterminada
const defaultRoute = 'borriquita';

// Esta página solo sirve como punto de entrada - redirigir a la versión con URL dinámica
return Astro.redirect(`/martos-2d/${defaultRoute}`);
---